%% DATUM RPI Students
clear;
close all;

%% Load in the data
[num,txt,raw] = xlsread('NoCourses.xlsx');
raw(:,2:36);

%% SAT and ACT score convertion

[m,n] = size(raw);
SATACT = raw(:,[45,46]);
for i = 1:m
    if SATACT(i,2) == 26
        i
    end
end

        